---
// Partial route fetched by the LazyContent styleguide demo — renders body-only
// markup (no layout/head), exactly how a real lazy modal fragment ships.
export const partial = true;
---

<div style='padding: 1rem 0'>
  <h3>Lazily fetched content</h3>
  <p class='sg-muted'>
    This block shipped from <code>/fragments/demo/</code> the first time the modal
    opened — not inline on the page.
  </p>
</div>
