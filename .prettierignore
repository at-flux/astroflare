# Generated by multi-semantic-release, in its own format. The release commit is
# `[skip ci]`, so a changelog it writes is never format-checked on the way in and
# only fails the gate once a later branch merges alongside it.
**/CHANGELOG.md

# Build output and dependencies.
**/dist/
**/.astro/
**/node_modules/
pnpm-lock.yaml
